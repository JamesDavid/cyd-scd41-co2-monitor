# PlatformIO build output
.pio/

# Arduino IDE build output (legacy)
cyd_scd41_co2_monitor_portrait/build/

# Exported compiled binaries
*.ino.esp32.bin
*.ino.bin
*.ino.elf
*.ino.map

# Firmware binaries served by the GitHub Pages web flasher MUST stay tracked.
# (Keep this negation below any *.bin ignore rule that might be added later.)
!docs/firmware/*.bin

# Editor / IDE
.vscode/

# Local data backups / stray artifacts (not project source)
# - downloaded /history.csv backups (often date-prefixed)
*co2_history*.csv
# - voice/audio scratch files
*.wav
# - stray root-level dup of 3D_Printed_Case/Aura+-+Case+with+Smooth+Front.3mf
/Aura+-+Case+with+Smooth+Front.3mf

# Claude Code local-only settings
.claude/settings.local.json

